# dotfiles

## Deploy on a new machine

```bash
curl -fsSL https://github.com/klawr/dotfiles/archive/master.tar.gz | tar -xz -C /tmp && bash /tmp/dotfiles-master/deploy.sh
```

## Update from local config

```bash
bash fetch.sh
```
